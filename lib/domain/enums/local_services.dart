enum LocalDataFieldType {
  STRINK_LIST,
  STRING,
  INT,
  DOUBLE,
  BOOL,
}

enum LocalDataFieldName {
  USER_PROFILE,
  USER_TOKEN,
  USER_PREFERRED_LANGUAGE,
  LAST_OPENED_DATA,
  USER_PREFERRED_VIEW_FACTOR,
  SEARCH_HISTORY,
}
